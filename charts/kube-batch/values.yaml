# Default values for kube batch.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
global:
  scheduler:
    type: "kube-batch"

replicaCount: 1
image:
  repository: kubesigs
  tag: v0.5
  pullPolicy: IfNotPresent
resources: {}
nodeSelector: {}