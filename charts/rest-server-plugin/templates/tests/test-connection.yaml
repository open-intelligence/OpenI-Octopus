apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rest-server-plugin.fullname" . }}-test-connection"
  labels:
{{ include "rest-server-plugin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "rest-server-plugin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
